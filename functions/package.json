{
  "name": "family-functions",
  "description": "우리家 이야기 Cloud Functions",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^6.6.0",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "private": true
}
